import 'package:flutter/material.dart';

class VehicleAddFiles extends StatelessWidget {
  const VehicleAddFiles({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
